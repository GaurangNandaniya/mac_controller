-r requirements.txt
pystray>=0.19.5
pycaw>=20230407
screen-brightness-control>=0.22.0
# winsdk only publishes pre-releases (latest is 1.0.0b10); ">=1.0.0" matches
# nothing and makes pip abort the whole install. Pin the beta explicitly.
winsdk==1.0.0b10
